extraction_queue_config = {
    "name": "resume:extraction-consumer",
    "exchange": "resume-exchange",
}

elastic_queue_config = {
    "name": "resume:elastic-consumer",
    "exchange": "resume-exchange",
}